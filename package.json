{
  "name": "chwazam",
  "version": "1.0.0",
  "description": "A fun finger chooser game with tower battle mechanics",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "npx playwright test --headed",
    "cap:sync": "npm run build && npx cap sync",
    "cap:android": "npm run build && npx cap sync && cd android && ./gradlew assembleDebug",
    "gen:icon": "node scripts/gen-icon.cjs",
    "gen:feature-graphic": "node scripts/gen-feature-graphic.cjs",
    "gen:screenshots": "npm run build && node scripts/gen-screenshots.cjs",
    "gen:store": "npm run gen:icon && npm run gen:feature-graphic && npm run gen:screenshots"
  },
  "dependencies": {
    "@capacitor/core": "^8.1.0"
  },
  "devDependencies": {
    "@capacitor/android": "^8.1.0",
    "@capacitor/cli": "^8.1.0",
    "@playwright/test": "^1.45.0",
    "canvas": "^3.2.1",
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  },
  "license": "WTFPL"
}
